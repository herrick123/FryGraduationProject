<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="sodb.directory" >
    <!-- 根据ID查询所有子节点-->
    <select id="findAllChildsByParentId" parameterType="java.lang.String" resultType="java.lang.String" >
        SELECT ID FROM SYS_TREE_MANAGE
        CONNECT BY PRIOR ID = PARENT_ID
        START WITH PARENT_ID = #{ID}
    </select>
    <update id="updateAllChildTreeCode" parameterType="java.lang.String">
        UPDATE SYS_TREE_MANAGE
        SET TREE_CODE = #{treeCode}
        WHERE ID = (SELECT ID FROM SYS_TREE_MANAGE
        CONNECT BY PRIOR ID = PARENT_ID
        START WITH PARENT_ID = #{id})
    </update>
    <!--更新子节点的上级目录名字  -->
    <update id="updateAllChildParentName" parameterType="java.lang.String">
        UPDATE SYS_TREE_MANAGE
        SET PARENT_NAME = #{name}
        WHERE ID in(SELECT ID FROM SYS_TREE_MANAGE
        WHERE PARENT_ID =#{id})
    </update>
    <!--根据ID查询所有子节点包括当前节点的ID  -->
    <select id="findAllThisAndChildsId" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT ID FROM SYS_TREE_MANAGE
        CONNECT BY PRIOR ID = PARENT_ID
        START WITH ID = #{id}
    </select>


</mapper>