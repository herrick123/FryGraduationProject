<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.userEntity" >
<select id="search_firmList_userEntity_info" parameterType="java.util.Map" resultType="com.self.user.entity.UserEntity" >
	SELECT
		a.uuid as uuid,
		a.USER_NAME as userName,
		a.ROLE as role,
		a.PHONE as phone,
		a.QQ as qq,
		a.ADDRESS as address,
		a.DELIVERY_ADDRESS as deliveryAddress
	FROM
		USER a
	WHERE
		1=1
		AND a.role = '2'
		<if test = "address != null" >
		AND a.ADDRESS like concat (concat ('%',#{address}),'%') 
		</if > 
</select>
</mapper>